export const TILE   = 64;   // pixels per grid square
export const COLS   = 10;   // grid width  (number of columns)
export const ROWS   = 10;   // grid height (number of rows)
export const HEADER = 56;   // px reserved above the maze for the month title
