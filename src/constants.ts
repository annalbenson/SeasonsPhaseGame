export const TILE   = 80;   // pixels per grid square
export const COLS   = 10;   // grid width  (number of columns)
export const ROWS   = 10;   // grid height (number of rows)
export const HEADER = 100;  // px reserved above the maze for the month title + quote
export const PANEL  = 160;  // right-side panel width (legend + objectives)
